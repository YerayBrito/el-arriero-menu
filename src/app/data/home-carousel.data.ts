/**
 * Fotos del carrusel de inicio (`public/assets/photos/`).
 * Para nitidez en pantallas grandes: ~1920 px de ancho (o más), formato JPG/WebP.
 * Para añadir más: copia la imagen a esa carpeta y agrega la ruta al array.
 */
export const HOME_CAROUSEL_IMAGES: readonly string[] = [
  '/assets/photos/Bar-1.jpg',
  '/assets/photos/Bar-3.jpg',
];

/** Intervalo entre slides (ms). */
export const HOME_CAROUSEL_INTERVAL_MS = 6000;
